Open Many React Projects Simultaneously

1. cd counter
	C:\Users\Admin\Documents\ian\PHPMySQL\Session 7 React Hooks\counter>
2. npm i
	- Will install packages
3. npm run dev -- --port 3000

4. open new terminal

5. cd pokedex
	C:\Users\Admin\Documents\ian\PHPMySQL\Session 7 React Hooks\pokedex>
6. npm i
	- Will install packages
7. npm run dev -- --port 3001

------ 
Clone

1. Set up a project folder
2. open vs code or terminal in the project folder
3. git clone URL .
4. git switch session_7
------ 
Pull

1. Make sure that you are in the root project
2. Make sure you are connected to the git repo
	> git remote -v
origin  https://github.com/cuckoodile/SHS-PHPMYSQL-2026-2027.git (fetch)origin  https://github.com/cuckoodile/SHS-PHPMYSQL-2026-2027.git (push)
3. Make sure you are at the correct  branch
	> git branch
* session_7
4. git fetch
5. git pull

------
Push

> VERIFICATION
1. Check if you are in the project terminal path (See if your terminal is in your project)
----
> PROJECT REPO SETUP
1. Create a PRIVATE repository named "{last_name} 2026-2027"
2. Invite me via "Collaborators" (cuckoodiles)
3. Copy report git URL (the one that ends on ".git")
----
> PUSH
1. git init
2. git remote  add origin {URL}
3. git add .
4. git commit -m "{message...}"
5. git branch -m session_7
6. git push -u origin session_7

----
> Activity

Overview:
Create a simple single paged web game that displays 10 boxes using a .map on an Array of boxes. Each box is unique (they have their own unique useState) but they all share the same functionality via custom component.

Instruction:
When you click a box, change the color of that selected box to the next color of your choice, the colors rotates infinitely.
